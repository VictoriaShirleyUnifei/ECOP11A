#ifndef VOLUME_H
#define VOLUME_H

// Funcao para calcular o volume de um cubo
float cubo(float aresta);

// Funcao para calcular o volume de um paralelepipedo
float paralelepipedo(float largura, float profundidade, float altura);

// Funcao para calcular o volume de uma esfera
float esfera(float raio);

// Funcao para calcular o volume de um cilindro
float cilindro(float raio, float altura);

// Funcao para calcular o volume de uma piramide
float piramide(float largura, float profundidade, float altura);

#endif