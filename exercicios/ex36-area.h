#ifndef AREA_H
#define AREA_H

// Funcao para calcular a area de um quadrado
float quadrado(float largura);

// Funcap para calcular a area de um retangulo
float retangulo(float largura, float profundidade);

// Funcao para calcular a area de um circulo
float circulo(float raio);

// Funcao para calcular a area de um triangulo
float triangulo(float largura, float profundidade);

#endif